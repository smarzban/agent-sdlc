# Verification report — evidence-gated-techstack

Terminal AC → proof map (ship stage). This feature is skill/reference prose with no product runtime, so
every AC is **reviewer-checked** (conformance re-read + the green bar as a run-and-read whole — no
test-backed rows, no name-appearance linkage). Each row records the answered question and its per-task
reviewer verdict. Checker (`sdlc-check … --require verification-report`) exit 0; suite 143 green;
`agent-sdlc/checker/` untouched in the diff (NC-1).

| Criterion | Type | Proof |
| --- | --- | --- |
| AC-1 | reviewer-checked | Does the techstack SKILL body mandate a runnable probe for load-bearing library claims, as a step + principle? PASS — checklist step 5 "Probe every load-bearing claim" (install → minimal script → run → keep output → reference; "research is not a probe") + choice-bar item 7 + a Principles bullet; T-2 reviewer confirmed all three present. |
| AC-2 | reviewer-checked | Does `techstack/reference/probing.md` exist, define the time-boxed throwaway spike, and does the SKILL body mandate reading it at the probe step? PASS — T-1 created probing.md (throwaway spike: never merged, findings feed Design/Tech Stack/ADRs, deleted after, no-pre-build guard); T-2 added the load-bearing "Read [reference/probing.md](reference/probing.md) now" mandate at step 5. Both reviewers confirmed. |
| AC-3 | reviewer-checked | Do all three boundary constraints appear in probing.md — (i) never mutates the product tree, (ii) time-boxed with the box stated, (iii) verified-by-probe is a CLAIM (ADR-0001), gate reads tag + output presence/shape only? PASS — all three stated in "## The three pinned boundary constraints"; T-1 reviewer verified each clause and the read-only framing. |
| AC-4 | reviewer-checked | Does the techstack artifact contract tag each load-bearing claim `verified-by-probe` (with a reference to the kept probe output) or `asserted`? PASS — "## The artifact (output)" Choices bullet defines the inline tag form `verified-by-probe -> <path/log/transcript>` (openable kept output) or `asserted`; T-2 reviewer confirmed the on-spec format is spelled out. |
| AC-5 | reviewer-checked | Does gate check 4 + a red flag carry the asserted-load-bearing-claim finding AND state the read-only tag-reading (gate reads tag + output presence/shape only, never runs the probe)? PASS — check 4 flags an asserted/untagged load-bearing claim as a finding routed to techstack; a matching red flag + done-when line; the read-only boundary is pinned verbatim ("checks the tag and the kept probe output's presence and shape ONLY — it never runs the probe and never authenticates its history"). T-3 reviewer confirmed the CRITICAL boundary clean. |
| AC-6 | reviewer-checked | Is the "No new products — reuses the declared stack" fast-path form defined in techstack, recognized in the gate walk, and framed as gate-recognized (not a checker rule)? PASS — defined in the techstack artifact contract (satisfies component→product for all components at once, a declared claim); recognized in gate check 1; framed verbatim as "a **gate-walk recognition, not a new `sdlc-check` rule**" — T-4 reviewer grep-verified the checker does not walk component→product (D3 true, not asserted). |
| AC-7 | reviewer-checked | Does the suite pass, does `sdlc-check` exit 0 on this spec, and is `agent-sdlc/checker/` untouched in the diff (enforcement is the gate walk, not a checker rule)? PASS — `node --test agent-sdlc/checker/*.test.mjs` = 143 pass / 0 fail at every task; `sdlc-check … --require ledger` and `--require verification-report` exit 0; `git diff main..HEAD -- agent-sdlc/checker/` is empty (NC-1 honored). |
