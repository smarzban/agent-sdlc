# Verification report — harness-captured-evidence

Terminal AC → proof map (ship stage). This feature is build-skill prose with no product runtime, so every
AC is **reviewer-checked** (conformance re-read + the green bar as a run-and-read whole). Each row records
the answered question and its per-task reviewer verdict. `sdlc-check … --require verification-report`
exit 0; suite 149 green; `agent-sdlc/checker/` untouched in the diff (NC-1).

| Criterion | Type | Proof |
| --- | --- | --- |
| AC-1 | reviewer-checked | Does the build SKILL per-task step mandate a conductor-run command whose actual output is captured, and forbid transcribing the subagent's report? PASS — step 4d now mandates "the conductor itself runs the full declared set … after the reviewer passes / before the commit" and records "that run's actual output … a harness-captured fact, never a transcription of the subagent's reported `Tests N passed` summary or count"; step 4f reinforces the fenced block is "the conductor's own run output, not a restatement of what the subagent said." T-1 reviewer confirmed genuine sharpening (not a restatement). |
| AC-2 | reviewer-checked | Does the SKILL require per-test names + state a tail/cap bound? PASS — step 4d requires the per-test `ok N - <name>` listing (the AC-14 matcher) and states a tail/cap bound for large suites that "retain[s] the per-test names of any test the proof map will cite" (build-complete comprehensive block as the fallback). T-1 reviewer confirmed the bound does not reintroduce the summary-only bug. |
| AC-3 | reviewer-checked | Are both holes named + closed, with a rationalization row + red flag? PASS — step 4d/the "Evidence is captured text" principle name (i) trust-the-subagent and (ii) summary-only degradation; a rationalization row ("The subagent already ran the tests and reported them green — I'll record its count." → rebuttal) and a red flag (a transcribed summary / bare `# pass N` count as evidence) were added. |
| AC-4 | reviewer-checked | Does subagent-loop.md make the conductor's post-acceptance run the authoritative captured evidence, consistent with the build SKILL? PASS — the Capture section was tightened from "one of the green-bar runs above is the task's evidence" to "the task's evidence is the conductor's own verification run … never a transcription of the implementer subagent's reported counts; the implementer's TDD runs are the subagent's business, not the recorded evidence." T-2 reviewer confirmed consistency with the sharpened build SKILL and that no correct existing content was removed. |
| AC-5 | reviewer-checked | Suite green, `sdlc-check` exit 0 on this spec, and `agent-sdlc/checker/` untouched in the diff? PASS — `node --test agent-sdlc/checker/*.test.mjs` = 149 pass / 0 fail at each task; `sdlc-check … --require ledger` and `--require verification-report` exit 0; `git diff 7b8a35a..HEAD -- agent-sdlc/checker/` is empty (NC-1 — SMA-463 is a build discipline, not mechanizable). |
